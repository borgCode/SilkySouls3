<Window x:Class="SilkySouls3.Views.ResistancesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:converters="clr-namespace:SilkySouls3.Converters"
        mc:Ignorable="d"
        Height="Auto" Width="180" 
        SizeToContent="Height"
        AllowsTransparency="True" 
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="#80000000">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <converters:ImmuneFillConverter x:Key="ImmuneFillConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
    </Window.Resources>
    
    <Border BorderBrush="#80333333" BorderThickness="1" CornerRadius="3" Margin="2">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <Button Content="✕" Width="16" Height="16" FontSize="8" 
                        HorizontalAlignment="Right" Background="Transparent" 
                        BorderThickness="0" Foreground="#CCFFFFFF"
                        Click="CloseButton_Click"/>
            </Grid>
            
            <StackPanel Grid.Row="1" Margin="0,0,0,5" 
                        Visibility="{Binding ShowPoise, Converter={StaticResource BoolToVis}}">
                <DockPanel Margin="0,0,0,2">
                    <TextBlock Text="Poise" Foreground="#CCFFFFFF" DockPanel.Dock="Left"/>
                    <TextBlock Foreground="#CCFFFFFF" HorizontalAlignment="Right" FontSize="10" DockPanel.Dock="Right">
                        <Run Text="{Binding TargetCurrentPoise, StringFormat={}{0:F1}, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TargetMaxPoise, StringFormat={}{0:F1}, Mode=OneWay}"/>
                        <Run Text="("/>
                        <Run Text="{Binding TargetPoiseTimer, StringFormat={}{0:F1}s, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                </DockPanel>
                <Grid>
                    <ProgressBar Height="12" Value="{Binding TargetCurrentPoise}" 
                                Maximum="{Binding TargetMaxPoise}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border BorderBrush="#80444444" BorderThickness="1" Background="#40222222" CornerRadius="2">
                                    <Grid x:Name="PART_Track">
                                        <Rectangle x:Name="PART_Indicator"
                                                Fill="#4CAF50"
                                                HorizontalAlignment="Left"
                                                RadiusX="1" RadiusY="1"
                                                Margin="1" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                </Grid>
            </StackPanel>
            
            <StackPanel Grid.Row="2" Margin="0,0,0,5" 
                        Visibility="{Binding ShowBleedAndNotImmune, Converter={StaticResource BoolToVis}}">
                <DockPanel Margin="0,0,0,2">
                    <TextBlock Text="Bleed" Foreground="#CCFFFFFF" DockPanel.Dock="Left"/>
                    <TextBlock Foreground="#CCFFFFFF" HorizontalAlignment="Right" FontSize="10"
                               Visibility="{Binding IsBleedImmune, Converter={StaticResource InverseBoolToVis}}" 
                               DockPanel.Dock="Right">
                        <Run Text="{Binding TargetCurrentBleed, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TargetMaxBleed, Mode=OneWay}"/>
                    </TextBlock>
                </DockPanel>
                <Grid>
                    <ProgressBar Height="12" Value="{Binding TargetCurrentBleed}" 
                                Maximum="{Binding TargetMaxBleed}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border BorderBrush="#80444444" BorderThickness="1" Background="#40222222" CornerRadius="2">
                                    <Grid x:Name="PART_Track">
                                        <Rectangle x:Name="PART_Indicator"
                                                Fill="{Binding IsBleedImmune, Converter={StaticResource ImmuneFillConverter}, ConverterParameter=#D32F2F}"
                                                HorizontalAlignment="Left"
                                                RadiusX="1" RadiusY="1"
                                                Margin="1" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock Text="IMMUNE" Foreground="#CCFFFFFF" FontSize="8" FontWeight="Bold" 
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsBleedImmune, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </StackPanel>
            
            <StackPanel Grid.Row="3" Margin="0,0,0,5" 
                        Visibility="{Binding ShowPoisonAndNotImmune, Converter={StaticResource BoolToVis}}">
                <DockPanel Margin="0,0,0,2">
                    <TextBlock Text="Poison" Foreground="#CCFFFFFF" DockPanel.Dock="Left"/>
                    <TextBlock Foreground="#CCFFFFFF" HorizontalAlignment="Right" FontSize="10"
                               Visibility="{Binding IsPoisonImmune, Converter={StaticResource InverseBoolToVis}}" 
                               DockPanel.Dock="Right">
                        <Run Text="{Binding TargetCurrentPoison, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TargetMaxPoison, Mode=OneWay}"/>
                    </TextBlock>
                </DockPanel>
                <Grid>
                    <ProgressBar Height="12" Value="{Binding TargetCurrentPoison}" 
                                Maximum="{Binding TargetMaxPoison}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border BorderBrush="#80444444" BorderThickness="1" Background="#40222222" CornerRadius="2">
                                    <Grid x:Name="PART_Track">
                                        <Rectangle x:Name="PART_Indicator"
                                                Fill="{Binding IsPoisonImmune, Converter={StaticResource ImmuneFillConverter}, ConverterParameter=#7B1FA2}"
                                                HorizontalAlignment="Left"
                                                RadiusX="1" RadiusY="1"
                                                Margin="1" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock Text="IMMUNE" Foreground="#CCFFFFFF" FontSize="8" FontWeight="Bold" 
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsPoisonImmune, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </StackPanel>
            
            <StackPanel Grid.Row="4" Margin="0,0,0,5" 
                        Visibility="{Binding ShowToxicAndNotImmune, Converter={StaticResource BoolToVis}}">
                <DockPanel Margin="0,0,0,2">
                    <TextBlock Text="Toxic" Foreground="#CCFFFFFF" DockPanel.Dock="Left"/>
                    <TextBlock Foreground="#CCFFFFFF" HorizontalAlignment="Right" FontSize="10"
                               Visibility="{Binding IsToxicImmune, Converter={StaticResource InverseBoolToVis}}" 
                               DockPanel.Dock="Right">
                        <Run Text="{Binding TargetCurrentToxic, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TargetMaxToxic, Mode=OneWay}"/>
                    </TextBlock>
                </DockPanel>
                <Grid>
                    <ProgressBar Height="12" Value="{Binding TargetCurrentToxic}" 
                                Maximum="{Binding TargetMaxToxic}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border BorderBrush="#80444444" BorderThickness="1" Background="#40222222" CornerRadius="2">
                                    <Grid x:Name="PART_Track">
                                        <Rectangle x:Name="PART_Indicator"
                                                Fill="{Binding IsToxicImmune, Converter={StaticResource ImmuneFillConverter}, ConverterParameter=#CE93D8}"
                                                HorizontalAlignment="Left"
                                                RadiusX="1" RadiusY="1"
                                                Margin="1" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock Text="IMMUNE" Foreground="#CCFFFFFF" FontSize="8" FontWeight="Bold" 
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsToxicImmune, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </StackPanel>
            
            <StackPanel Grid.Row="5" Margin="0,0,0,0" 
                        Visibility="{Binding ShowFrostAndNotImmune, Converter={StaticResource BoolToVis}}">
                <DockPanel Margin="0,0,0,2">
                    <TextBlock Text="Frost" Foreground="#CCFFFFFF" DockPanel.Dock="Left"/>
                    <TextBlock Foreground="#CCFFFFFF" HorizontalAlignment="Right" FontSize="10"
                               Visibility="{Binding IsFrostImmune, Converter={StaticResource InverseBoolToVis}}" 
                               DockPanel.Dock="Right">
                        <Run Text="{Binding TargetCurrentFrost, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TargetMaxFrost, Mode=OneWay}"/>
                    </TextBlock>
                </DockPanel>

                <Grid>
                    <ProgressBar Height="12" Value="{Binding TargetCurrentFrost}" 
                                Maximum="{Binding TargetMaxFrost}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border BorderBrush="#80444444" BorderThickness="1" Background="#40222222" CornerRadius="2">
                                    <Grid x:Name="PART_Track">
                                        <Rectangle x:Name="PART_Indicator"
                                                Fill="#2196F3"
                                                HorizontalAlignment="Left"
                                                RadiusX="1" RadiusY="1"
                                                Margin="1" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock Text="IMMUNE" Foreground="#CCFFFFFF" FontSize="8" FontWeight="Bold" 
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsFrostImmune, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>